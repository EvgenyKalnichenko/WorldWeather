import { createStore } from 'vuex'
import cities from './modules/cities'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    cities
  }
})
